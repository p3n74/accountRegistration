<?php
require_once 'EventController.php';

class EventsController extends EventController {
    // This class serves as an alias so that routes starting with /events map correctly.
    // All functionality is inherited from EventController.
} 